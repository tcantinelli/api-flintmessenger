import { Profile } from "../models/profiles";

const profilesController = {

	// update(id: string, email: string, firstname: string, lastname: string) {
	// 	const upUser = userController.find(id);
	// 	const upIndex = userExistants.findIndex(user => user.id == id);

	// 	if (upUser) {
	// 		if (prenom && upUser.prenom !== prenom) upUser.prenom = prenom;
	// 		if (nom && upUser.nom !== nom) upUser.nom = nom;
	// 		if (email && upUser.email !== email) upUser.email = email;

	// 		userExistants.splice(upIndex, 1, upUser);

	// 		return upUser;
	// 	}

	// 	return null;
	// },
}